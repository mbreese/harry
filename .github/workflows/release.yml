name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build all release binaries
        run: make release

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            bin/harry-darwin-arm64
            bin/harry-linux-amd64
            bin/harry-linux-arm64
            bin/harry-server-darwin-arm64
            bin/harry-server-linux-amd64
            bin/harry-server-linux-arm64
